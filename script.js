// Function to handle system login clicks
function login(system) {
    // For now, show an alert
    alert(`Redirecting to ${system} login page...`);

    // Replace this with actual login URLs if needed
    // Example:
    // if(system === 'puregold') window.location.href = 'puregold_login.php';
    // else if(system === 'tnap') window.location.href = 'tnap_login.php';
    // else if(system === 'fishermall') window.location.href = 'fishermall_login.php';
}
